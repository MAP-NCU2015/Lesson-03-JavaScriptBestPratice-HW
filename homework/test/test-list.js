describe('Test Math.pow ', function() {
  beforeEach(function() {
  });

  it('would calculate correct value', function() {
    // Write any pure function assertion here.
    assert.equal(729, Math.pow(3, 6));
  });
});
